var int a, x;

fun int sumarec(int x)
  if (x < 1) then
    return (0)
  else
    return (x + sumarec(x-1))
  endif
endfun

fun int suma(int x)
  var int accum;
  accum = 0;
  while 0 < x do
    accum = accum + x; x = x - 1
  endwhile;
  print(accum);
  return (accum)
endfun

fun void main()
  var int y;
  x = 5;
  y = suma(3);
  print(y);
  return ()
endfun
